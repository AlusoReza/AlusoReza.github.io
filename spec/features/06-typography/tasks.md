# 06 · Typography — Tasks

- [x] Add Google Fonts `<link>` tags in BaseLayout.astro
- [x] Define `--font-display` and `--font-body` in `:root`
- [x] Apply font variables to all relevant selectors
- [x] Verify `display=swap` is set in font URL
- [ ] Verify fonts load correctly in browser (devtools → network → fonts)
- [ ] Verify no FOIT on throttled connection (devtools → throttling → Slow 3G)

## Maintenance

- [ ] If adding a new text element, assign the correct font variable
- [ ] Do NOT mix fonts in the same context (display for titles, body for text)
