# 07 · Computational grid — Tasks

- [x] Implement grid background on `#inicio` with two linear gradients
- [x] Add `::after` pseudo-element for fade gradient
- [x] Add responsive override at 650px (20px spacing)
- [x] Set `pointer-events: none` on `::after`
- [ ] Verify grid only appears on `#inicio` (not on other sections)
- [ ] Verify grid renders correctly on mobile

## Maintenance

- [ ] Do NOT replicate the grid in other sections — it's a signature exclusive to the hero
- [ ] If the hero layout changes significantly, verify the fade gradient still works
