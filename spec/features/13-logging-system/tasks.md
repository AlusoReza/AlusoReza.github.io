# 13 · Logging system — Tasks

- [x] Create `docs/logs/` directory structure
- [x] Create `docs/bitacora.md` global summary
- [x] Document logging protocol in AGENTS.md
- [x] Implement initial snapshot (git diff) before first change
- [ ] Verify logging protocol after every build (self-check)

## Maintenance

- [ ] No exceptions — the check runs after EVERY build
- [ ] When reading historical context, always scan `docs/logs/` and `docs/bitacora.md`
