# 02 · Data contracts — Tasks

- [x] Create all 7 JSON files with bilingual format
- [x] Implement `t()` helper in client.js with fallback chain
- [x] Implement `toggleSection()` for empty arrays
- [ ] Run `check-json-schema.ps1` to validate all contracts

## Maintenance

- [ ] When adding a new field to any JSON, update the spec and run tests
- [ ] When adding a new data file, add static import to `BaseLayout.astro` and bundle in `data-data`
