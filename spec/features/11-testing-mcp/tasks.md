# 11 · MCP Tests — Tasks

- [x] Create `check-astro-mcp.ps1` (16 checks)
- [x] Create `check-frontend-design.ps1` (22 checks)
- [x] Create `check-js-logic.ps1` (~7 checks)
- [x] Create `check-css-logic.ps1` (~5 checks)
- [x] Create `check-json-schema.ps1` (~13 checks)
- [x] Create `check-paths.ps1` (~7 checks)
- [x] Create `run-all.ps1` master runner with bugs.md integration
- [ ] Run `run-all.ps1` and verify all checks pass/fail as expected

## Maintenance

- [ ] When adding a new feature, add corresponding checks to the relevant module
- [ ] When installing a new MCP server, check if any test becomes obsolete
