# Code decisions — <Project Name>

Critical code decisions. Each entry documents what was decided, why, what bug it fixes, what happens if reverted, and any related frontend decision (if any).

**Format:** One entry per critical decision. Each entry includes file:line reference, session, decision, bug fixed, and revert consequence.

## 1. <Decision title>
- **File:** `<path/to/file>` L<line range>
- **Session:** <number>
- **Decision:** <What was decided and the technical approach taken>
- **Bug fixed:** <What problem this solves or what would happen without it>
- **Revert consequence:** <What breaks if this decision is undone>
