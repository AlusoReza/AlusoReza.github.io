# 2026-07-19 — Session Log

---

## Sesión 70 — Responsive MobileProfile layout

### Prompt
Rediseñar el MobileProfile para que sea responsivo dentro del rango mobile (0–1235px). El contenido debe disponerse horizontalmente en anchos amplios y progresivamente apilarse verticalmente al reducir el viewport. Sin tocar la transición PC↔mobile (grid collapse, sidebar-locked). La foto no debe ser demasiado pequeña.

### Plan
3 estados responsivos con flexbox:
- **STATE 3** (<500px): Stack vertical centrado (comportamiento actual). Foto 120×150px.
- **STATE 2** (500–699px): Dos filas — foto+texto a la izquierda, acciones centradas abajo. Foto 100×125px.
- **STATE 1** (700–1235px): Fila horizontal completa — foto | texto | acciones. Foto 100×125px.

### Changes
- `src/components/MobileProfile.astro` — Reestructurar HTML: envolver nombre+desc en `.mobile-profile-text`, social+CV en `.mobile-profile-actions`.
- `src/styles/global.css` — Reemplazar estilos base de mobile-profile (líneas 224-337) con sistema flex responsivo. Añadir sección `5a. Mobile Profile — Responsive Layout` con 3 breakpoints. Comentarios `>>> PHOTO SIZES` para ajuste local de dimensiones de foto.

### Build
```
npm run build
1 page(s) built in 634ms
Complete!
```

---

## Sesión 77 — Fix sidebar blank on maximize

### Prompt
Sidebar aparece en blanco al maximizar ventana desde estado minimizado. No hay transición, el sidebar se queda vacío hasta que "carga" el contenido. El problema es `sidebar-locked` creando un hueco de 350ms donde el sidebar tiene width:0 y opacity:0.

### Changes
- `src/scripts/client.js` — Eliminar `sidebar-locked` y su timer de la dirección de crecimiento (mobile→desktop). La fórmula CSS `--sidebar-fade` maneja la transición naturalmente
- `src/styles/global.css` — Eliminar `.is-resizing .sidebar` de la regla `transition: none !important` (solo queda `.is-resizing .sidebar-overlay`)

### Build
```
npm run build
1 page(s) built in 638ms
Complete!
```
0 errors, 0 warnings.

---

## Sesión 71 — JS-driven responsive MobileProfile layout

### Prompt
El usuario quiere3 estados de disposición: (1) 4 columnas `Photo|Name|Title|Actions` cuando cabe, (2) 3 columnas `Photo|Name/Title|Actions` cuando ya no cabe, (3) todo vertical. Los botones sociales siempre encima del botón CV. Detectar los cambios de tamaño con JS (ResizeObserver) en vez de CSS media queries.

### Plan
Reemplazar los breakpoints CSS por clases toggleadas vía `ResizeObserver`:
- `PROFILE_ROW_MIN = 650px` → añade `.mobile-profile-inner--row` (3 columnas)
- `PROFILE_TEXT_ROW_MIN = 800px` → añade `.mobile-profile-text--row` (4 columnas)
- Las acciones siempre `flex-direction: column` (social arriba, CV abajo)

### Changes
- `src/styles/global.css` — Eliminar bloques `@media` de STATE 2/1 (líneas 361-405). Añadir estilos baseados en clases `.mobile-profile-inner--row` y `.mobile-profile-text--row`.
- `src/scripts/client.js` — Añadir bloque `ResizeObserver` (~15 líneas) después de `updateMobileProfile()`. Mide `.mobile-profile-inner` y togglea clases según ancho.

### Build
```
npm run build
1 page(s) built in 1.08s
Complete!
```
0 errors, 0 warnings.

---

## Sesión 72 — Fix mobile profile text positioning and reflow

### Prompt
El título se superpone al nombre. Al hacer resize los textos cambian de tamaño (reflow). Quitar translateX(20px) de los apellidos. Asegurar que los textos no cambien de disposición durante resize.

### Changes
- `src/styles/global.css` — 4 fixes: (1) `gap: 4px` + `min-width: 0` en `.mobile-profile-text`, (2) Eliminar `translateX(20px)` de `.mobile-profile-name-last`, (3) `white-space: nowrap` en `.mobile-profile-name span` y `.mobile-profile-desc`, (4) `flex: 0 0 auto` en `.mobile-profile-text--row .mobile-profile-name`.

### Build
```
npm run build
1 page(s) built in 649ms
Complete!
```

---

## Sesión 73 — Grid restructure + 3 fixes for mobile profile layout

### Prompt
Título pegado al nombre en STATE 1, se superpone en vez de ir debajo al achicar, STATE 2 tiene espacio extra. Se quiere: título como bloque dinámico centrado entre nombre y CV.

### Plan
1. HTML: Sacar `.mobile-profile-desc` de `.mobile-profile-text` → hijo directo de `.mobile-profile-inner`
2. CSS: Flexbox → CSS Grid en `.mobile-profile-inner`
3. JS: `mobile-profile-text--row` → `mobile-profile-inner--wide` aplicado en inner
4. Fix STATE 1: `justify-items: center` + `text-align: center` en `--wide`
5. Fix overlap: quitar `white-space: nowrap` de `.mobile-profile-desc`
6. Fix STATE 2: row gap `8px` → `4px`

### Changes
- `src/components/MobileProfile.astro` — desc moved outside `.mobile-profile-text`
- `src/styles/global.css` — flex→grid, 3 states (1fr / auto 1fr auto / auto auto 1fr auto), STATE 1 child placement, 3 fixes
- `src/scripts/client.js` — ResizeObserver toggles `mobile-profile-inner--wide` on inner instead of `mobile-profile-text--row` on text

### Build
```
npm run build
1 page(s) built in 634ms
Complete!
```

---

## Sesión 74 — Dynamic STATE 1 threshold + constant title size

### Prompt
El título reflowea al achicar la ventana. Se quiere tamaño constante sin reflow, y que salte directo de STATE 1 a STATE 2 cuando el contenido no cabe.

### Changes
- `src/styles/global.css` — Restaurar `white-space: nowrap` en `.mobile-profile-desc` para tamaño constante
- `src/scripts/client.js` — Eliminar `PROFILE_TEXT_ROW_MIN = 800` fijo. ResizeObserver ahora mide `scrollWidth` de nombre, título y botones, calcula `totalNeeded = 200 + nameW + descW + actionsW + 60`. Solo activa `inner--wide` cuando `w >= totalNeeded`.

### Build
```
npm run build
1 page(s) built in 821ms
Complete!
```

---

## Sesión 75 — Dual-desc with show/hide

### Prompt
STATE 1 y STATE 2 están rotos: se superponen, el título queda pegado al nombre, el nombre en STATE 2 sale vertical en vez de horizontal. Se quiere: STATE 1 = commit exacto (foto | nombre vertical | título separado centrado | botones), STATE 2 = bloque combinado centrado (foto | [nombre horizontal + título] centrado | botones). Solución: dos elementos desc — uno inline dentro de text-block (STATE 2) y otro standalone fuera (STATE 1) — con show/hide por estado.

### Changes
- `src/components/MobileProfile.astro` — Añadir `<p class="mobile-profile-desc mobile-profile-desc--inline">` dentro de `.mobile-profile-text`. Renombrar desc original a `mobile-profile-desc--standalone`
- `src/styles/global.css` — Default: `.mobile-profile-desc--standalone { display: none }`. STATE 2 (--row): rewrite completo — grid `auto 1fr auto`, centered, text block flex column centered, nombre `flex-direction: row` + `gap: 8px`, standalone hidden. STATE 1 (--wide): inline hidden, standalone grid-placed in col 3
- `src/scripts/client.js` — Selector cambia de `.mobile-profile-desc` a `.mobile-profile-desc--standalone` para medir el desc correcto para el threshold

### Build
```
npm run build
1 page(s) built in 636ms
Complete!
```

---

## Sesión 76 — Unify STATE 1 + STATE 2 into single horizontal state

### Prompt
Reformular: un solo estado horizontal (nombre a lo largo + título debajo, bloque centrado) que salta a vertical cuando el contenido no cabe. Sin STATE 1 vs STATE 2, sin dual-desc.

### Changes
- `src/components/MobileProfile.astro` — Eliminar standalone desc. Queda solo un desc dentro de `.mobile-profile-text`. Eliminar clase `--inline`
- `src/styles/global.css` — Eliminar bloque `--wide` completo (48 líneas). Eliminar regla `.mobile-profile-desc--standalone { display: none }`. El bloque `--row` existente ya tiene el layout correcto
- `src/scripts/client.js` — Eliminar `PROFILE_ROW_MIN`. Medir spans del nombre individualmente (`scrollWidth` de cada span + 8px gap). Calcular `totalNeeded = PHOTO_W + Math.max(nameW, descW) + actionsW + gaps`. Toggle solo `--row`

### Build
```
npm run build
1 page(s) built in 634ms
Complete!
```
