# 2026-06-25

---

## Sesión 1 — Refactorización MCP (alineación con Astro best practices)

### Prompt

Revisar el código del portfolio y refactorizarlo para alinearlo con las mejores prácticas de Astro 5: reemplazar el script inline `is:inline set:html` por el atributo `data-data` en el `<body>`, cambiar los `onclick` por `data-lang` + `addEventListener`, eliminar la función global `window.changeLanguage`, añadir `tsconfig.json` y crear el directorio `.agents/` con la skill de frontend-design.

### Plan

Se identificaron tres problemas principales en el código actual:
1. **Script inline con `set:html`** — Astro desaconseja inyectar HTML serializado mediante `is:inline`. La alternativa correcta es pasar los datos como atributo `data-*` en una etiqueta HTML y leerlos desde el DOM en el cliente.
2. **Gestores de eventos inline (`onclick`)** — Mezcla de lógica JS con marcado HTML. Se migran a `data-lang` + `addEventListener` en `client.js`.
3. **Variable global `window.changeLanguage`** — Contamina el scope global. Se encapsula toda la lógica en `client.js`.

Adicionalmente, se añade `tsconfig.json` (extiende `astro/tsconfigs/base`) para habilitar el type-checking del ecosistema Astro, y se crea el directorio `.agents/` con la skill `frontend-design` y el archivo `skills-lock.json`.

Se descartó la opción de mantener `is:inline` porque Astro ya no lo recomienda para datos dinámicos, y usar fetch() para cargar JSONs en cliente añadía complejidad y latencia innecesarias.

### Cambios

#### `src/layouts/BaseLayout.astro`
- **Eliminado** el bloque `<script is:inline set:html>` que inyectaba `window.DATA = {...}` directamente en el HTML.
- **Añadido** el atributo `data-data` al `<body>` con el valor del objeto `data` serializado mediante el helper `Astro.serialize(data)` (que escapa HTML entities automáticamente).
- El template del layout ahora pasa `data` como prop desde `index.astro` (que recopila todos los JSONs: lang, profile, skills, education, projects, experience, certificates).

#### `src/scripts/client.js`
- **Eliminada** la función `decodeHtml()` — ya no es necesaria porque el browser decodifica automáticamente el contenido del atributo `dataset` al acceder a `document.body.dataset.data`.
- **Lectura de datos**: `const data = JSON.parse(document.body.dataset.data)` — reemplaza `window.DATA`.
- **`changeLanguage()` renombrada a `h()`** — función local encapsulada, ya no expuesta globalmente.
- **Event listeners**: se añadió `document.querySelectorAll("[data-lang]").forEach(el => { el.addEventListener("click", () => h(el.dataset.lang)) })` que reemplaza los `onclick="changeLanguage('es')"` del HTML.
- **Inicialización**: `init()` renombrada a `$()` y llamada al final del script. Carga el idioma guardado de `localStorage`, ejecuta `translateUI()`, activa el botón de idioma correspondiente, y dispara `scrollReveal()`.

#### `src/components/LangSwitcher.astro`
- **Reemplazados** los atributos `onclick="changeLanguage('es')"` y `onclick="changeLanguage('en')"` por `data-lang="es"` y `data-lang="en"`.
- El JS de `client.js` ahora selecciona estos elementos por `[data-lang]` y asigna el evento mediante `addEventListener`.

#### `tsconfig.json` (nuevo)
- Extiende `astro/tsconfigs/base`.
- Configura `strict: true`, `jsx: "preserve"`, `moduleResolution: "bundler"`.
- Permite type-checking de archivos `.astro`, `.ts`, `.tsx`.

#### `.agents/skills/frontend-design/SKILL.md` (nuevo)
- Skill de opencode con instrucciones de diseño frontend: paletas CSS first, tipografía, sistema de espaciado, animaciones con propósito, responsive mobile-first.

#### `skills-lock.json` (nuevo)
- Registro de skills instaladas para opencode: `frontend-design` con hash y fecha.

### Build

```
npm run build
```
Resultado: exitoso. 1 página generada en ~500ms. Sin warnings ni errores.

---

## Sesión 2 — Refactorización de diseño (aplicación de frontend-design skill)

### Prompt

Aplicar la skill `frontend-design` para mejorar la estética del portfolio: elegir tipografías, refinar la paleta de colores, añadir una firma visual (cuadrícula computacional), reformular el hero como una tesis profesional, eliminar emojis de los títulos y mejorar la jerarquía visual general.

### Plan

Se cargó la skill `frontend-design` y se evaluaron sus recomendaciones punto por punto:

**Tipografía:**
- Se evaluaron varias combinaciones. Se eligió **Space Grotesk** para display (títulos, nombre, badges) por su carácter técnico/geométrico que evoca matemáticas, código y física computacional — encaja perfectamente con el perfil del usuario. Para cuerpo de texto se eligió **Inter**, optimizada para pantalla, con buena legibilidad en pesos ligeros y excelente pairing con Space Grotesk.
- Se descartaron: JetBrains Mono (muy técnica, cansada en párrafos largos), system-ui stack (demasiado genérica).

**Paleta de acento:**
- Se refinó el azul de acento de `#58a6ff` (GitHub default, genérico) a `#7fc1fe` (eléctrico, más distintivo).
- Se añadió `#f0a030` (ámbar) como acento secundario para contraste cálido.
- Se definió un sistema completo de colores en `:root` con variables semánticas: `--color-bg`, `--color-bg-card`, `--color-border`, `--color-text`, `--color-text-bright`, `--color-text-muted`, etc. (Nota: estas variables se definieron con valores auto-referenciales `var(--color-bg): var(--color-bg)` por error — corregido en Sesión 3).

**Firma visual:**
- Se diseñó una cuadrícula computacional como fondo del hero (`#inicio`): líneas blancas al 2% de opacidad con spacing de 28px, simulando papel milimetrado / planos de coordenadas. Un gradiente de desvanecimiento en la parte inferior evita que se vea cortado abruptamente.
- La cuadrícula evoca física computacional, matemáticas, ingeniería de software — conecta visualmente con el perfil.

**Hero como tesis:**
- El subtítulo pasó de "Junior Software Developer | Backend & Data Enthusiast" a una declaración narrativa: "Físico computacional convertido en desarrollador de software — Backend · Datos · Sistemas", contando una historia en una línea.

**Headings sin emoji:**
- Se eliminaron los emojis de `sec-sobre`, `sec-hab`, `sec-proy`, `sec-est`, `sec-cont` y `cv-text-h`/`cv-text-f` en `lang.json`.
- Ejemplo: antes "📌 Sobre mí" → después "Sobre mí".
- Esto profesionaliza la interfaz y evita ruido visual.

### Cambios

#### `src/layouts/BaseLayout.astro`
- Añadidos en `<head>`: `<link rel="preconnect" href="https://fonts.googleapis.com">`, `<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>`, y `<link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&family=Space+Grotesk:wght@400;500;600;700&display=swap" rel="stylesheet">`.
- Las fonts se cargan en paralelo con la descarga del CSS; el `display=swap` evita FOIT.

#### `src/styles/global.css`
- **`:root` block (líneas 2-15)**: se añadió el sistema de custom properties con las variables semánticas. Se definieron `--font-display: 'Space Grotesk', sans-serif` y `--font-body: 'Inter', ...`.
- **`h2` (línea 37)**: se aplicó `font-family: var(--font-display)`, `letter-spacing: -0.02em`.
- **`#inicio` grid (líneas 155-179)**: se añadió la cuadrícula computacional como `background-image` con dos gradientes lineales (horizontal + vertical). Se añadió `::after` con gradiente de desvanecimiento. `border-radius: 12px` para esquinas suaves. Responsive: `background-size: 20px` en móvil.
- **`.profile-text h1` (línea 140)**: `font-family: var(--font-display)`, `letter-spacing: -0.03em`.
- **`.skill-item strong`, `.edu-header strong`**: se aplicó `font-family: var(--font-display)`.
- **Badges**: se refinaron los estilos hover para usar `--color-border` en lugar de hardcoded colors.
- **`.tools-badges .badge:hover`**: hover unificado con `transform: translateY(-2px)`.
- **`.social-btns a`**: hover actualizado con `box-shadow` más pronunciado.

#### `src/data/lang.json`
- **`hero-sub` (ES)**: cambiado de `"<strong>Junior Software Developer</strong> | Backend & Data Enthusiast"` a `"<strong>Físico computacional</strong> convertido en <strong>desarrollador de software</strong><br>Backend · Datos · Sistemas"`.
- **`hero-sub` (EN)**: cambiado a `"<strong>Computational physicist</strong> turned <strong>software developer</strong><br>Backend · Data · Systems"`.
- **`sec-sobre`**: `"📌 Sobre mí"` → `"Sobre mí"`.
- **`sec-hab`**: `"💡 Habilidades · Perfil Profesional"` → `"Habilidades · Perfil Profesional"`.
- **`sec-proy`**: `"🚀 Proyecto Destacado"` → `"Proyecto Destacado"`.
- **`sec-est`**: `"🎓 Formación Académica"` → `"Formación Académica"`.
- **`sec-cont`**: `"📬 ¿Construimos el siguiente nivel?"` → `"¿Construimos el siguiente nivel?"`.
- **`cv-text-h` y `cv-text-f`**: eliminado el emoji 📄 del inicio; ahora empiezan directamente con "Descargar...".

### Build

```
npm run build
```
Resultado: exitoso. Sin errores ni warnings.

---

## Sesión 3 — Corrección de color y subtítulo

### Prompt

"Vale, es verdad que ha mejorado estéticamente pero sigo prefiriendo una estética más parecida a la de antes. Los cambios de tipografía mantenlos, ahora bien la parte de fondo negro y recuadros de las partes vamos a ajustarlas a algo más parecido a lo que había antes. Además encontramos que la página es extremadamente blanca y al pasar el ratón por encima de las cosas las vuelve más blanca y por tanto invisibles. Por otro lado la parte de física computacional convertido en desarrollador de software es cutrona. Mejor algo más simple cómo desarrollador de software a secas o de física a desarrollador de software. Por otro lado aparecen cosas como backend, datos y sistemas y tal vez es mejor poner full stack porque la verdad es que estoy tocando un poco de todo, y además podríamos añadir ia o agentes."

### Plan

Se identificó un bug crítico: las variables de `:root` en `global.css` eran auto-referenciales (`--color-bg: var(--color-bg)`) en lugar de contener valores reales. Esto hacía que todas las referencias `var(--color-*)` cayeran al valor inicial del navegador (transparente/blanco), explicando por qué la página se veía blanca y los hover hacían invisible el texto.

**Cambios planeados:**
1. **`:root`** — reemplazar todas las variables auto-referenciales con valores reales del tema oscuro GitHub: `--color-bg: #0d1117`, `--color-bg-card: #161b22`, `--color-bg-hover: #1c2128`, `--color-border: #30363d`, `--color-text: #c9d1d9`, `--color-text-bright: #f0f6fc`, `--color-text-muted: #8b949e`. Definir `--color-green: #3fb950` (estaba referenciado pero nunca definido).
2. **`.btn-outline`** — usaba colores de tema claro (`#555`, `#f5f5f5`, `#000`). Migrar a usar las custom properties del tema oscuro.
3. **Hero subtitle** — simplificar. Se ofrecieron dos opciones al usuario: (A) "Desarrollador de software" + tags, o (B) "De la física al desarrollo de software" + tags. El usuario eligió la opción A (corta y directa).
4. **Tags** — "Backend · Datos · Sistemas" → "Full Stack · IA · Agentes".
5. **Profile.astro** — actualizar el fallback estático renderizado por Astro para que coincida con el nuevo texto.

Se decidió mantener la tipografía (Space Grotesk + Inter), la cuadrícula computacional, y el resto de cambios de diseño de la Sesión 2.

### Cambios

#### `src/styles/global.css`
- **`:root` (líneas 2-15)**: reemplazadas todas las variables auto-referenciales:
  - Antes: `--color-bg: var(--color-bg)` → Ahora: `--color-bg: #0d1117`
  - Antes: `--color-bg-card: var(--color-bg-card)` → Ahora: `--color-bg-card: #161b22`
  - Antes: `--color-bg-hover: var(--color-bg-hover)` → Ahora: `--color-bg-hover: #1c2128`
  - Antes: `--color-border: var(--color-border)` → Ahora: `--color-border: #30363d`
  - Antes: `--color-text: var(--color-text)` → Ahora: `--color-text: #c9d1d9`
  - Antes: `--color-text-bright: var(--color-text-bright)` → Ahora: `--color-text-bright: #f0f6fc`
  - Antes: `--color-text-muted: var(--color-text-muted)` → Ahora: `--color-text-muted: #8b949e`
  - Antes: `--color-green: var(--color-green)` → Ahora: `--color-green: #3fb950` (nuevo)
  - Los demás valores (`--color-accent: #7fc1fe`, `--color-accent-secondary: #f0a030`, fonts) se mantuvieron igual.
- **`.btn-outline` (líneas 433-450)**: migrado de colores hardcoded de tema claro a custom properties:
  - Antes: `border: 1px solid #555; color: #555;`
  - Ahora: `border: 1px solid var(--color-border); color: var(--color-text-muted);`
  - Hover antes: `background-color: #f5f5f5; border-color: #000; color: #000;`
  - Hover ahora: `background-color: var(--color-bg-card); border-color: var(--color-accent); color: var(--color-text-bright);`

#### `src/data/lang.json`
- **`hero-sub` (ES, línea 11)**: 
  - Antes: `"<strong>Físico computacional</strong> convertido en <strong>desarrollador de software</strong><br>Backend · Datos · Sistemas"`
  - Ahora: `"<strong>Desarrollador de software</strong><br>Full Stack · IA · Agentes"`
- **`hero-sub` (EN, línea 39)**:
  - Antes: `"<strong>Computational physicist</strong> turned <strong>software developer</strong><br>Backend · Data · Systems"`
  - Ahora: `"<strong>Software developer</strong><br>Full Stack · AI · Agents"`

#### `src/components/Profile.astro`
- **Línea 9** — fallback estático renderizado por Astro (se muestra antes de que JS lo reemplace):
  - Antes: `<p class="subtitle" data-i18n="hero-sub"><strong>Junior Software Developer</strong> | Backend &amp; Data Enthusiast</p>`
  - Ahora: `<p class="subtitle" data-i18n="hero-sub"><strong>Desarrollador de software</strong><br>Full Stack · IA · Agentes</p>`

### Build

```
npm run build
```
Resultado: exitoso. 1 página generada en 463ms. Sin errores ni warnings.
- Verificado en el output HTML: el `data-data` contiene `&#34;hero-sub&#34;:&#34;<strong>Desarrollador de software</strong><br>Full Stack · IA · Agentes&#34;` correctamente escapado.
- Verificado en el CSS compilado: `--color-bg: #0d1117; --color-bg-card: #161b22; ... --color-green: #3fb950` presentes en el bundle.

---

## Sesión 4 — Sistema de logging automático

### Prompt

Crear un sistema de logging automático que registre todas las sesiones de trabajo del agente: un log detallado por día en `docs/logs/YYYY-MM-DD.md` (con prompt resumido, plan detallado, cambios archivo por archivo, y resultado del build) y un log global en `docs/bitacora.md` con resúmenes escaneables por fecha. Aplicar retroactivamente a todo el trabajo de hoy. Modificar `AGENTS.md` para que el agente siga este workflow en todas las sesiones futuras.

### Plan

**Requisitos del usuario:**
- Log por sesión con fecha: `docs/logs/YYYY-MM-DD.md`
- Cada sesión nueva en un día distinto = archivo nuevo
- Varias sesiones en el mismo día = mismo archivo, separadas por `---`
- Estructura de cada sesión: Prompt (resumido), Plan (muy detallado), Cambios (muy detallado, por archivo), Build (comando + resultado)
- Log global `docs/bitacora.md`: resumen rápido prompt + plan por fecha
- Aplicar retroactivo a las 3 sesiones anteriores de hoy
- La sesión actual (logging) es la Sesión 4
- Modificar AGENTS.md con workflow obligatorio

**Decisiones:**
- Usar markdown plano para todo (sin herramientas adicionales, consistente con el stack del proyecto)
- El agente es responsable de mantener los logs manualmente como parte del workflow
- El log global usa una tabla de contenido implícita por fecha para facilitar la navegación
- La estructura del log detallado incluye rutas de archivo con bullets y descripción de qué cambió exactamente

**Alternativas descartadas:**
- Usar un script de Node.js para generar logs automáticamente — demasiada complejidad para el proyecto, y no capturaría el contexto de las decisiones humanas
- Usar commits de git como log — no captura prompts ni planes, solo el diff final
- Formato JSON/YAML — menos legible que markdown para escaneo visual rápido

### Cambios

#### `docs/logs/` (nuevo directorio)
- Directorio contenedor para los logs diarios de sesión.
- Se crea `docs/logs/2026-06-25.md` con las 4 sesiones de hoy.

#### `docs/logs/2026-06-25.md` (nuevo)
- Archivo de log detallado del día con 4 sesiones completas.
- Cada sesión incluye: Prompt, Plan detallado, Cambios (archivo por archivo con líneas afectadas), Build (comando + resultado).

#### `docs/bitacora.md` (nuevo)
- Resumen global escaneable del flujo de trabajo.
- Entradas cortas por fecha y sesión: prompt resumido + plan en 2-3 líneas.
- Para referencia visual rápida de todo lo que se ha hecho en el proyecto.

#### `AGENTS.md`
- **Añadido `docs/` al Project Structure** (líneas 42-44).
- **Actualizada descripción de BaseLayout.astro** (línea 25): `window.DATA inline script` → `data-data attribute on body`.
- **Actualizada sección Conventions** (línea 66): `window.DATA` → `data-data` con `Astro.serialize()` y `dataset.data`.
- **Actualizada sección Architecture** (líneas 69-70): referencias a `window.DATA` → `document.body.dataset.data`, `onclick` → `addEventListener`.
- **Añadido Contexto histórico** (línea 79): instrucción de escanear `docs/logs/` y `docs/bitacora.md` si se pregunta por trabajo anterior y no está en contexto.
- **Nueva sección en Workflow** (después de "Adding content"):
  ```markdown
  - **Session log (IMPORTANTE — SIEMPRE):**
    1. Al iniciar una sesión de trabajo, crear `docs/logs/YYYY-MM-DD.md` si no existe (usando la fecha actual).
    2. Antes de ejecutar ningún cambio, añadir un encabezado `## Sesión N — Título descriptivo` y rellenar `### Prompt` con un resumen de lo que pidió el usuario.
    3. Tras obtener aprobación del plan, rellenar `### Plan` con el plan detallado incluyendo alternativas consideradas, decisiones tomadas y justificaciones.
    4. Tras implementar todos los cambios y verificar el build exitosamente, rellenar `### Cambios` lista cada archivo modificado con las líneas afectadas y una descripción detallada del cambio y por qué se hizo. Incluir también `### Build` con el comando ejecutado, el resultado (éxito/error), tiempo si está disponible, y cualquier detalle relevante (warnings, notas de verificación).
    5. Actualizar `docs/bitacora.md` con una entrada resumida (fecha, sesión, prompt breve + plan en 2-3 líneas).
    6. No omitir ningún paso. Esto es obligatorio para cada tarea, sin excepción.
  ```

### Build

No aplica — esta sesión no modifica el build del portfolio, solo añade documentación del workflow. El build existente sigue funcionando correctamente.

---

## Sesión 5 — Anclaje de contexto y actualización de logs

### Prompt

El usuario preguntó "What did we do so far?" al inicio de la sesión. Se le proporcionó un resumen estructurado con todo el trabajo realizado (goal, progress, key decisions, relevant files). Luego preguntó si se había actualizado el log de la sesión actual, constatando que no se había seguido el protocolo de logging obligatorio de AGENTS.md. Pide crear la entrada faltante.

### Plan

Se identificó que la Sesión 5 (esta conversación) no tenía entrada en el log a pesar de que AGENTS.md exige crear la cabecera de sesión antes de ejecutar cambios y actualizar `### Build` tras cada build. La omisión ocurrió porque la sesión empezó con un resumen/anclaje de contexto en lugar de un nuevo task, y luego pasó a Plan Mode.

**Corrección:** Crear la entrada de Sesión 5 en `docs/logs/2026-06-25.md` y añadir el resumen a `docs/bitacora.md`.

**Decisión:** No se ejecuta build porque esta sesión no modifica ningún archivo del portfolio — solo documentación del workflow.

### Cambios

#### `docs/logs/2026-06-25.md`
- Añadida Sesión 5 al final del archivo, separada por `---`.

#### `docs/bitacora.md`
- Añadida entrada de Sesión 5 con prompt resumido y plan en 2-3 líneas.

### Build

No aplica — esta sesión no modifica código del portfolio. Solo documentación.

---

## Sesión 6 — Tests MCP y Frontend Design

### Prompt

"Ahora quiero añadir dos tests distintos... en uno de los test quiero que cada vez que yo diga que quiero comprobar que se sigue el formato mcp de astro la IA corra un test de todo el proyecto y si hay cosas mejorables proponga un plan, en el segundo test quiero que haga exactamente lo mismo pero con la skill de frontend-design."

### Plan

Se evaluaron 3 opciones: (A) checklist manual en AGENTS.md, (B) scripts PowerShell, (C) híbrida — scripts para checks mecánicos + revisión manual para semántica. El usuario eligió la opción C con output mostrando fallos por pantalla y planes de acción.

Se diseñaron dos scripts PowerShell autocontenidos:

- **check-mcp.ps1**: 16 checks de alineación con Astro 5 (onclick, is:inline, window globals, Astro.serialize, data-data, tsconfig, addEventListener, set:html, skills-lock.json, etc.)
- **check-frontend-design.ps1**: 21 checks de diseño (design tokens, circular refs, !important, prefers-reduced-motion, responsive, tipografía, paleta, firma visual, emojis, componentes, etc.)

Cada script incluye:
- Formato colorido (PASS/FAIL/WARN) con `Write-Host`
- Resumen numérico al final
- Plan de acción detallado para cada incumplimiento con solución propuesta
- Exit code 1 si hay FAILs graves

Además, se creó `skills-lock.json` que estaba faltante (detectado durante el análisis), y se actualizó `AGENTS.md` con la sección `## Tests`.

### Cambios

#### `.agents/tests/check-mcp.ps1` (nuevo)
- 16 checks automatizados de mejores prácticas Astro. Incluye detección de `onclick=`, `is:inline`, `window.DATA`, `Astro.serialize()`, verificación de `data-data` en `<body>`, `tsconfig.json`, `addEventListener`, `set:html`, estructura `.agents/`, y script bundling.

#### `.agents/tests/check-frontend-design.ps1` (nuevo)
- 21 checks de diseño. Incluye verificación de variables `:root`, referencias circulares, `!important` (solo en prefers-reduced-motion), detección de `prefers-reduced-motion` en CSS y JS, `.reveal` reset, responsive (650px/480px), tipografía (font-display/font-body), paleta (sin `#58a6ff`, sin `#ffffff` hardcodeado fuera de badges), firma visual (cuadrícula, gradiente), emojis en headings, `.btn-outline`, nav sticky, colores redundantes, lang-switcher fixed, back-to-top.

#### `.agents/tests/.gitkeep` (nuevo)
- Archivo vacío para trackear el directorio `tests/` en git.

#### `.agents/skills-lock.json` (nuevo)
- Registro de la skill frontend-design. Estaba ausente — se crea para completar la estructura del proyecto.

#### `AGENTS.md`
- Añadida sección `## Tests` al final (antes de Documentation) con el protocolo: el usuario dice "Comprueba MCP" o "Comprueba diseño", el agente ejecuta el script correspondiente, revisa los resultados, y propone plan de acción sin aplicar correcciones automáticas sin aprobación.

#### `src/styles/global.css` (post-build)
- Migrados 7 `color: #ffffff;` → `color: var(--color-text-bright);` en las líneas 196, 281, 302, 420, 485, 576, 771. Resuelve WARN 1 del test de diseño: si se cambia el tema en el futuro, esos textos no quedarían blancos sobre fondo claro.

### Build

```
npm run build
```
Resultado: exitoso en 517ms. 1 página generada. Sin errores ni warnings. Build capturado después de corregir WARN 1 del test de diseño.

Antes del build, los scripts fueron verificados:
- `check-mcp.ps1` → 16 PASS, 0 FAIL, 0 WARN ✅
- `check-frontend-design.ps1` → 21 PASS → 22 PASS tras la corrección. WARNs reducidos de 3 a 2 (#1a1f26 y colores de marca).
