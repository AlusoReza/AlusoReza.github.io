# 2026-06-29 — Session 11

## Prompt
Reemplazar el PDF `Guia-Markdown-Curso-Desarrollo-con-IA-Programa-con-Agentes.PDF` en `docs/certificates/` por el certificado real, extraer sus datos y agregarlo a `src/data/certificates.json`.

## Plan
1. Leer el nuevo PDF (`Certificado-Alonso-Jose-Suarez-Reza-32z2bo0a.pdf`) e intentar extraer metadatos y texto.
2. Confirmar con el usuario los datos del certificado (no extraíbles automáticamente por ser imagen sin OCR).
3. Insertar el entry en `src/data/certificates.json` como 2º elemento (orden cronológico: entre Python y AWS).
4. Build y verify.

## Changes
- **`src/data/certificates.json`**: Insertado nuevo certificado de "Certificado de Iniciación al Desarrollo con IA: Programa con Agentes" de BIG SCHOOL (26 jun. 2026, 6h) como 2º elemento del array.

## Build
- `npm run build` → OK (863ms, 1 page, no errors/warnings)

---

# 2026-06-29 — Session 12

## Prompt
Revisar bugs.md y roadmap.md, elaborar plan para corregir todos los bugs y ejecutarlo.

## Plan
See approved plan in spec (9 bugs + 2 mobile layout fixes):
1. Fix `init()` no `renderAll()` — client.js
2. Fix 📄 icon in Contact.astro (separate from data-i18n)
3. Fix `btn-primary` → `btn-outline` — Projects.astro + client.js
4. Fix lang switcher overflow — global.css @media 480px
5. Fix social buttons width — global.css @media 650px
6. Fix `rel="noopener noreferrer"` — Contact.astro + client.js
7. Fix `changeLanguage` early return — client.js
8. Fix `#1a1f26` → `var(--color-bg-card)` — global.css
9. Fix relative CV paths → absolute — profile.json + Contact.astro

## Changes
All fixes applied in single pass:

- **`src/scripts/client.js`**: 4 changes — `init()` calls `renderAll()` if lang ≠ 'es'; early return in `changeLanguage`; `btn-primary` → `btn-outline`; added `rel="noopener noreferrer"` to project links
- **`src/components/Contact.astro`**: 3 changes — separated 📄 icon from `data-i18n` span; added `rel="noopener noreferrer"` to LinkedIn & GitHub links; CV path changed to absolute `/assets/...`
- **`src/components/Projects.astro`**: 2 changes — `btn-primary` → `btn-outline`; added `rel="noopener noreferrer"`
- **`src/styles/global.css`**: 3 changes — `#1a1f26` → `var(--color-bg-card)`; added `@media (max-width: 480px)` for lang switcher compact layout; added `min-width: 160px` for social buttons at 650px
- **`src/data/profile.json`**: CV path changed to absolute `/assets/Alonso_Reza_CV.pdf`
- **`spec/constitution/bugs.md`**: Cleaned completely — 10 bugs moved to ✅ Fixed with session details; remaining 4 items marked as INFO (intentional/neutral)
- **`spec/constitution/roadmap.md`**: Next items moved to Done

## Build
- `npm run build` → OK (456ms, 1 page, no errors/warnings)
- `.agents/tests/run-all.ps1` → **0 FAIL, 8 WARN** (down from 4 FAIL, 20 WARN). All remaining WARNs are intentional/neutral: brand colors, quote style, compound CSS classes, empty experience.

---

# 2026-06-29 — Session 13

## Prompt
Añadir LaTeX como language badge, Overleaf y Excel como tool badges. Añadir Bootcamp de Metodologías Ágiles como 3er certificado. Extraer info del curso del CV.

## Plan
1. Añadir badge `b-latex` (teal border-accent) a profile.json y CSS
2. Añadir badges `b-overleaf` (Overleaf green) y `b-excel` (Excel green) con estilo solid+glow a profile.json y CSS
3. Añadir bootcamp UNIR 2025 como 3er entry en certificates.json (sin PDF)
4. Build + tests

## Changes
- **`src/data/profile.json`**: +1 language badge (LaTeX), +2 tool badges (Overleaf, Excel)
- **`src/styles/global.css`**: +1 border-accent CSS class (`.b-latex`), +2 solid+glow CSS classes (`.b-overleaf`, `.b-excel`)
- **`src/data/certificates.json`**: +1 entry — "Bootcamp en Habilidades Digitales y Gestión Ágil" (UNIR, 2025) como 3er elemento

## Build
- `npm run build` → OK (481ms, 1 page, no errors/warnings)
- `.agents/tests/run-all.ps1` → **0 FAIL, 8 WARN** (same as Session 12 — all intentional/neutral)
- `certificates.json`: 5 certificates, correct format ✅ (previously 4)
- `profile.json`: complete with 12 badges ✅

---

# 2026-06-29 — Session 14

## Prompt
Añadir XML/JSON y Batch/Bash como language badges (2 paquetes de 2). Cambiar badge "AI Agents" → "OpenCode/Claude" con color naranja. Cambiar subtitle "Full Stack · IA · Agentes" → "Full Stack · Agentes · Data".

## Plan
1. profile.json: +2 badges XML/JSON y Batch/Bash en badges[]; cambiar label+class de AI Agents → OpenCode/Claude en toolBadges[]
2. global.css: +2 border-accent CSS classes (`.b-xml-json`, `.b-batch-bash`); renombrar `.b-aiagents` → `.b-opencode-claude` con naranja #d97757
3. lang.json: cambiar hero-sub ES y EN
4. Build

## Changes
- **`src/data/profile.json`**: +XML/JSON, +Batch/Bash (language badges); AI Agents → OpenCode/Claude (tool badge)
- **`src/styles/global.css`**: +`.b-xml-json` (#e67e22), +`.b-batch-bash` (#4CAF50); `.b-aiagents` → `.b-opencode-claude` (#d97757 naranja)
- **`src/data/lang.json`**: hero-sub ES → "Full Stack · Agentes · Data"; EN → "Full Stack · Agents · Data"

## Build
- `npm run build` → OK (486ms, 1 page, no errors/warnings)

---

# 2026-06-29 — Session 15

## Prompt
Hacer una comprobación completa del proyecto en busca de más bugs.

## Plan
1. Run `run-all.ps1` (0 FAIL, 8 WARN — same intentional)
2. Deep manual review via sub-agent: check previous fixes, new badges, hardcoded colors, console errors, i18n, a11y, dead code, build, cert PDFs
3. Fix any bugs found

## Changes
- **`src/components/Profile.astro:9`**: Updated stale static fallback from "Full Stack · IA · Agentes" → "Full Stack · Agentes · Data" to match lang.json
- **`.agents/tests/run-all.ps1:12-17`**: Updated [MANUAL] items — removed 2 stale entries (init flow, 📄 icon — both fixed in S12), added 2 relevant items (static fallback freshness, badge CSS orphans)

## Bugs found
- **1 real bug (Medium):** Stale static fallback text in Profile.astro:9 — showed "Full Stack · IA · Agentes" while lang.json had "Full Stack · Agentes · Data". Fixed.
- All previous fixes (S12) verified working. All new badges have CSS classes. No console errors. i18n complete. Build clean.

## Build
- `npm run build` → OK (485ms, 1 page, no errors/warnings)
- `.agents/tests/run-all.ps1` → **0 FAIL, 8 WARN** (all intentional/neutral)

---

# 2026-06-29 — Session 16

## Prompt
Ocultar "Experiencia" de la barra de navegación cuando no haya datos. Aplicar también para "Certificados".

## Plan
1. Importar `experience.json` y `certificates.json` en Nav.astro
2. Condicionar los links con spread operator (si hay datos → mostrar; si no → ocultar)
3. Build + verify

## Changes
- **`src/components/Nav.astro`**: Links de experiencia y certificados ahora condicionales:
  - `showExperience = experience && experience.length > 0` → show nav-exp link
  - `showCertificates = certificates && certificates.length > 0` → show nav-cert link
  - Mismo patrón que `Experience.astro` y `toggleSection()` en client.js

## Build
- `npm run build` → OK (469ms, 1 page, no errors/warnings)
- Nav output: Inicio, Sobre mí, Habilidades, Proyectos, Estudios, Certificados, Contacto
- "Experiencia" oculta en nav (experience.json vacío). Aparecerá automáticamente cuando haya datos.
- "Certificados" visible (5 entries). Se ocultará automáticamente si el array queda vacío.

---

# 2026-06-29 — Session 17

## Prompt
El bug del lang-switcher que se sale de la barra y los botones sociales desbordando la pantalla sigue ocurriendo.

## Plan
1. Mover lang-switcher dentro de `<nav>` como elemento inline (eliminar `position: fixed`)
2. Restructurar nav con flexbox (`.nav-links`) para integrar los botones ES/EN
3. Añadir `box-sizing: border-box` a `.social-btns a` y `.cv-cta-button` para evitar overflow por padding
4. Ajustar reglas responsive: `flex: 1 1 200px` en 650px, `width: 100%; max-width: 280px; flex: none` en 480px
5. Actualizar tests que verificaban el viejo approach (fixed position, LangSwitcher.astro)
6. Build + run-all.ps1

## Changes
- **`src/components/Nav.astro`**: Añadido `.lang-switcher` con botones ES/EN dentro de `.nav-links`
- **`src/layouts/BaseLayout.astro`**: Eliminada importación y renderizado de `<LangSwitcher />`
- **`src/styles/global.css`**:
  - `nav`: padding reducido de 15px → 10px
  - Nuevo `.nav-links`: `display: flex; flex-wrap: wrap; justify-content: center; align-items: center; gap: 4px`
  - `.nav-links a`: en vez de `nav a` general, con `padding: 5px 10px; white-space: nowrap`
  - `.lang-switcher`: eliminado `position: fixed; top: 15px; right: 20px` → ahora `display: flex; margin-left: 6px;` dentro de nav-links
  - `.social-btns a`: añadido `box-sizing: border-box; justify-content: center`
  - `.cv-cta-button`: añadido `box-sizing: border-box`
  - `@media (max-width: 650px)`: `.nav-links a` con font-size 0.75em; `.social-btns a` con `min-width: 0; flex: 1 1 200px` (reemplaza `min-width: 160px`)
  - `@media (max-width: 480px)`: `.nav-links a` con font-size 0.7em, `.social-btns a` con `width: 100%; max-width: 280px; flex: none`
  - Eliminado `padding-right: 80px` workaround del nav-container
  - Eliminados @media obsoletos para lang-switcher fixed (top/right/padding)
- **`.agents/tests/check-mcp.ps1`**: CHECK 10 actualizado — verifica Nav.astro en vez de LangSwitcher.astro
- **`.agents/tests/check-frontend-design.ps1`**: CHECK 19 actualizado — verifica `margin-left` en lang-switcher en vez de `position: fixed`
- **`spec/constitution/bugs.md`**: Limpiado (eliminados ~200 líneas de duplicados automáticos). Bugs de lang-switcher y social buttons actualizados a "Fixed: Session 17"

## Build
- `npm run build` → OK (543ms, 1 page, no errors/warnings)
- `.agents/tests/run-all.ps1` → **0 FAIL, 8 WARN** (all intentional/neutral)
- Nav output: `<nav> <div class="nav-container"> <div class="nav-links"> <a>...<a> <div class="lang-switcher"><button>ES</button>|<button>EN</button></div> </div> </div> </nav>`
- Lang-switcher ya no flota fuera del nav — es parte del flujo flexbox

---

# 2026-06-29 — Session 18

## Prompt
Crear un template en `spec/template/` con el plan de acción completo (workflow) para que cualquier IA pueda replicar nuestra forma de trabajar. Incluir la relación entre `docs/bitacora.md` y `docs/logs/`.

## Plan
1. Crear `spec/template/workflow-template.md` con 9 secciones: Session Lifecycle, Plan-First Protocol, Build-Driven Logging Protocol (incluyendo file hierarchy y relación bitácora/logs), Bug Tracking, Testing, Commit, Language Policy, Context Recovery, Quality Gates
2. Actualizar `spec/template/spec_template/README.md` para referenciar el nuevo workflow template
3. Build + tests + limpiar bugs.md + log

## Changes
- **`spec/template/workflow-template.md`** (nuevo): Protocolo completo de trabajo para cualquier IA:
  - **Session Lifecycle**: secuencia exacta de 11 pasos por sesión
  - **Plan-First Protocol**: cómo estructurar un plan, cuándo es necesario
  - **Build-Driven Logging Protocol**: file hierarchy (`docs/bitacora.md` como índice global, `docs/logs/YYYY-MM-DD.md` como detalle), relación entre ambos (bitácora es el mapa, logs son el territorio), ciclo BEFORE/AFTER con git snapshot, formato de la bitácora
  - **Bug Tracking Protocol**: detección, documentación, ciclo de fix, "never lose a bug"
  - **Testing Protocol**: cómo ejecutar tests, interpretar output (PASS/FAIL/WARN), qué hacer con findings
  - **Commit Protocol**: solo cuando el usuario pida, mensaje descriptivo con secciones
  - **Language Policy**: inglés para AI docs, idioma original para session logs
  - **Context Recovery**: escanear bitácora + logs antes de responder sobre sesiones anteriores
  - **Quality Gates**: checklist de 6 condiciones para dar una tarea por terminada
- **`spec/template/spec_template/README.md`**: Añadida sección "Workflow para la IA" y referencia a workflow-template.md en la estructura

## Build
- `npm run build` → OK (567ms, 1 page, no errors/warnings)
- `.agents/tests/run-all.ps1` → **0 FAIL, 8 WARN** (all intentional/neutral)
- `bugs.md` limpiado de duplicados automáticos tras el test run
