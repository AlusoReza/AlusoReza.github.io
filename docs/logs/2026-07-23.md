# 2026-07-23

## Session 139: Certificate cards — emojis + technology tags

### Prompt
Mejorar las tarjetas de Certificados. Anadir emoji al inicio del titulo que de una idea del curso, y anadir etiquetas (tags) de lenguajes/herramientas/frameworks en el espacio entre titulo y fecha.

### Plan
1. Anadir campo `tags` (array bilingue) a cada entrada en `certificates.json` + emoji en `title`.
2. Actualizar `renderCertificateItem()` en `client.js` para renderizar tags como pills.
3. Actualizar `Certificates.astro` para el render estatico del fallback.
4. Anadir CSS `.card-tags` y `.card-tag` (pills discretos debajo del titulo).

### Changes
- `src/data/certificates.json`: 13 entradas actualizadas con emoji en `title` + nuevo campo `tags` (array de objetos `{es, en}`).
- `src/scripts/client.js:147-162`: Renderiza `item.tags` como `<div class="card-tags">` con pills `<span class="card-tag">`.
- `src/components/Certificates.astro:11-20`: Mismo render para fallback estatico.
- `src/styles/global.css:1100-1120`: Nuevas reglas `.card-tags` (flex wrap, gap 6px) y `.card-tag` (pill 0.7rem, bg hover, border muted).

### Build
`npm run build` — exitoso (1.29s), 0 errors. Tests: 0 FAILs, 14 WARNs (preexistentes). No regressions.

---

## Session 140: Certificate cards — accent border + glow + tag refinement

### Prompt
Mejorar las tarjetas de certificados para que no queden tan sosas. Propuesta: borde izquierdo accent, hover glow, tags con color accent, institucion como divider.

### Plan
Solo CSS (4 reglas modificadas):
1. `.card-item` — `border-left: 3px solid var(--color-accent)` + padding ajustado
2. `.card-item:hover` — `box-shadow` accent glow reemplaza shadow generico
3. `.card-tag` — `background: var(--color-accent-muted)` + `color: var(--color-accent)` + border accent
4. `.card-sub` — `border-top: 1px solid var(--color-border)` + `padding-top: 10px` como divider

### Changes
- `src/styles/global.css`: 4 reglas modificadas (card-item, card-item:hover, card-tag, card-sub)

### Build
`npm run build` — exitoso (961ms), 0 errors. Tests: 0 FAILs, 14 WARNs (preexistentes). No regressions.

---

## Session 141: Reduce card accent intensity — hierarchy correction

### Prompt
Reducir la intensidad del verde accent en el borde izquierdo de las cards y en los tags. Lo mas importante es el titulo y la institucion. El resto da fatiga visual.

### Changes
- `src/styles/global.css`: 2 reglas modificadas:
  - `.card-item` border-left: `var(--color-accent)` → `rgba(100, 255, 218, 0.3)` (70% menos intenso)
  - `.card-item:hover` glow: `0.08` → `0.04` (mas sutil)
  - `.card-tag`: revertido a colores muted (bg: `--color-bg-hover`, color: `--color-text-muted`, border: `--color-border`)

### Build
`npm run build` — exitoso (611ms), 0 errors. Tests: 0 FAILs, 14 WARNs (preexistentes). No regressions.

---

## Session 144: Education cards — image layout + initials fallback + progressive collapse

### Prompt
Mejorar las tarjetas de Formación. Las cards de educación se diferencian de las de certificados con layout imagen+texto: imagen a la izquierda, contenido a la derecha. Si no hay imagen, mostrar iniciales. Mobile: imagen arriba, contenido abajo.

### Plan
1. `education.json`: anadir campos `image`, `initials`, `list` (logros destacados)
2. `Education.astro`: renderizado con imagen + fallback + card-content wrapper
3. `global.css`: reglas `.card-item--with-image`, `.card-image`, `.card-image-fallback`, `.card-content`
4. `client.js`: `renderEducationItem()` con onerror fallback (imagen → iniciales)
5. CSS shared: `.card-title` min-width 120px, `.card-tags` flex-shrink 1, progressive collapse

### Changes
- `src/data/education.json`: campos `image` (vacio), `initials` (UNIR/UGR), `list` (3 logros cada uno)
- `src/components/Education.astro`: renderizado con ternario imagen/fallback, wrapper `.card-content`
- `src/styles/global.css`: 6 nuevas reglas (card-item--with-image, card-image, card-image-fallback, card-content, mobile responsive)
- `src/scripts/client.js:88-112`: `renderEducationItem()` con onerror fallback y card-content wrapper
- `.card-title`: `min-width: 0` → `120px` (threshold para collapse)
- `.card-tags`: `flex-shrink: 0` → `1` (permite encoger antes de envolver)

### Build
`npm run build` — exitoso (611ms), 0 errors. Tests: 0 FAILs, 14 WARNs (preexistentes). No regressions.

---

## Session 145: Card mobile — emoji reposition + left-align + collapse fix

### Prompt
Tres bugs en cards mobile: 1) contenido no alineado a la izquierda al envolver (~25px margen), 2) emojis visuales en titulos mobile, 3) resize no activa collapse (titulo se comprime en vez de envolver).

### Plan
1. CSS: eliminar `margin-left: 16px` de `.card-tags` y `.card-date` (gap ya maneja spacing)
2. CSS: cambiar `.card-tags` a `flex-shrink: 0` (nunca encoge, solo envuelve)
3. JS: extraer emoji de titulo con regex `^[\p{Emoji_Presentation}\p{Extended_Pictographic}]*`, envolver en `<span class="card-emoji">`
4. CSS: `.card-emoji` inline desktop, `display: block` above title mobile via `flex-direction: column`

### Changes
- `src/styles/global.css:1063-1082`: `.card-title` min-width 120px + `.card-emoji` (inline desktop, block mobile con flex-direction column)
- `src/styles/global.css:1088`: `.card-date` eliminado `margin-left: 16px`
- `src/styles/global.css:1126`: `.card-tags` eliminado `margin-left: 16px`, `flex-shrink: 1` → `0`
- `src/scripts/client.js:152-156`: regex extrae emoji, renderiza `<span class="card-emoji">` + titulo sin emoji

### Build
`npm run build` — exitoso (600ms), 0 errors. Tests: 0 FAILs, 16 WARNs (preexistentes). No regressions.

---

## Session 146: Card title immutability — flex: 0 0 auto + text wrap as last resort

### Prompt
El título sigue comprimiéndose. Quiere que el título sea inmutable (ancho natural) y que SOLO envuelva sus palabras cuando el contenedor sea más estrecho que el título. Tags y fecha envuelven primero.

### Plan
Cambiar `.card-title` de `flex: 1; min-width: 120px` a `flex: 0 0 auto; max-width: 100%; overflow-wrap: break-word; word-break: break-word`.

### Changes
- `src/styles/global.css`: `.card-title` — `flex: 1` → `flex: 0 0 auto`, eliminado `min-width: 120px`, añadido `max-width: 100%`, `overflow-wrap: break-word`, `word-break: break-word`

### Build
`npm run build` — exitoso (703ms), 0 errors. Tests: 0 FAILs, 16 WARNs (preexistentes). No regressions.

### Prompt
Mover los tags de debajo del titulo a la misma linea: |Titulo|Tags|Fecha|. Separacion de 16px entre tags y fecha.

### Changes
- `src/scripts/client.js:147-162`: Tags ahora se renderizan dentro de `.card-header` (entre titulo y fecha)
- `src/components/Certificates.astro:11-25`: Mismo cambio en Astro
- `src/styles/global.css`: 4 reglas modificadas:
  - `.card-header`: eliminado `justify-content: space-between` (flex:1 en titulo maneja el espacio)
  - `.card-title`: anadido `flex: 1; min-width: 0` (expande para llenar)
  - `.card-tags`: anadido `flex-shrink: 0; margin-left: 16px` (compacto, separado del titulo)
  - `.card-date`: anadido `flex-shrink: 0; margin-left: 16px` (separa de tags)

### Build
`npm run build` — exitoso (574ms), 0 errors. Tests: 0 FAILs, 14 WARNs (preexistentes). No regressions.

---

## Session 143: Tags with DevIcon logos + initials fallback + softer accent

### Prompt
Mejorar tags de certificados: anadir iconos DevIcon (como tech grid de Sobre mi) o color verde suave. Se aplica opcion C: iconos donde haya DevIcon + fallback con initials + color accent suave.

### Changes
- `src/data/certificates.json`: Campo `logo` anadido a cada tag (CDN URL o string vacio). 10 tags con logo DevIcon (Python, AWS, Azure, Java, Spring, Docker, Rust, Git, GitHub, Spring Boot), 17 sin logo.
- `src/scripts/client.js:147-168`: Renderiza `<img>` si `tag.logo` es truthy, `<span class="card-tag-fallback">` con 2 primeras letras si no.
- `src/components/Certificates.astro:11-27`: Mismo render con ternario.
- `src/styles/global.css`: 3 nuevas reglas:
  - `.card-tag`: `inline-flex`, `gap:4px`, accent suave (`rgba(100,255,218,0.08)` bg, `0.55` text, `0.12` border)
  - `.card-tag img`: 14x14px DevIcon
  - `.card-tag-fallback`: 14x14px initials box, accent-muted bg

### Build
`npm run build` — exitoso (611ms), 0 errors. Tests: 0 FAILs, 14 WARNs (preexistentes). No regressions.

---

## Session 147: CSS Grid card header — 4-stage responsive collapse

### Prompt
Rediseñar el layout de `.card-header` para que en desktop: titulo a la izquierda (con emoji), tags centrados en el medio, fecha a la derecha. Colapso progresivo en 4 etapas cuando el contenedor se achica: (1) todo en linea, (2) tags+fecha en fila 2, (3) vertical, (4) emoji arriba del titulo.

### Changes
- `src/styles/global.css`: Cambio de flexbox a CSS Grid en `.card-header`:
  - `.card-header`: `display: grid; grid-template-columns: auto 1fr auto` (3 columnas)
  - `.card-title`: `grid-column: 1; justify-self: start` (izquierda)
  - `.card-tags`: `grid-column: 2; justify-self: center` (centro)
  - `.card-date`: `grid-column: 3; justify-self: end` (derecha)
  - Eliminado media query de 650px para emoji (se movio a stage 4)
  - Stage 2 (700px): `grid-template-columns: auto 1fr` — tags izquierda, fecha derecha
  - Stage 3 (480px): `grid-template-columns: 1fr` — todo vertical
  - Stage 4 (350px): `.card-title` flex column + `.card-emoji` block (emoji arriba)

### Build
`npm run build` — exitoso (867ms), 0 errors. Tests: 0 FAILs, 16 WARNs (preexistentes). No regressions.

---

## Session 148: CSS source order fix — .card-tags misplaced after media queries

### Prompt
Bugs al hacer resize: tags centradas en vez de izquierda, titulo debajo de tags, layout roto. Analisis: regla base `.card-tags` estaba DESPUES de las media queries (linea 1162), por lo que anulaba los overrides de stage 2/3.

### Changes
- `src/styles/global.css`: Movido bloque `.card-tags` base (lineas 1161-1168) a posicion 1090, DESPUES de `.card-date` y ANTES de las media queries. Ahora las media queries pueden override correctamente.

### Build
`npm run build` — exitoso (636ms), 0 errors. Tests: 0 FAILs, 16 WARNs (preexistentes). No regressions.
