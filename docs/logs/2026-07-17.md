# 2026-07-17

## Sesión 44 — Unificar "Sobre mí" en un solo bloque con saltos de párrafo desde código

### Prompt
Unificar los 3 párrafos del "Sobre mí" (sobre-p1, sobre-p2, sobre-p3) en un solo bloque de texto, con los saltos de párrafo controlados desde el código del componente, no desde claves de datos separadas.

### Plan
1. Fusionar `sobre-p1`, `sobre-p2`, `sobre-p3` en un solo campo `sobre-text` en `about.json` (separados por `\n\n`)
2. Reemplazar los 3 `<p data-i18n>` por un solo `<div data-i18n="sobre-text">` en `About.astro`, que splittea por `\n\n` y renderiza `<p>` para cada fragmento
3. Modificar `translateUI()` en `client.js` para detectar `sobre-text` y convertir `\n\n` → `<p>` tags al cambiar idioma

### Changes
- **src/data/about.json**: `sobre-p1` + `sobre-p2` + `sobre-p3` → `sobre-text` (con `\n\n` como separador de párrafo). Labels `about-lang-label`, `about-frameworks-label`, `about-tools-label` intactos.
- **src/components/About.astro:57-61**: Reemplazado 3 `<p data-i18n="sobre-p1/p2/p3" set:html={...}>` por `<div data-i18n="sobre-text"><p>...</p></div>` con `split('\n\n').map()`
- **src/scripts/client.js:15-22**: `translateUI()` ahora detecta `key === 'sobre-text'` y renderiza los párrafos con `split('\n\n').map(p => '<p class="about-paragraph reveal">...</p>')`

### Bug fix: sobre-mí desaparece al cambiar idioma
- **Causa:** `changeLanguage()` llamaba `translateUI()` dos veces — una dentro de `renderAll()` y otra al final. La segunda llamada destruía los `<p>` que el IntersectionObserver ya estaba observando y creaba nuevos sin observar, dejándolos con `opacity: 0`.
- **Fix:** Eliminar la llamada duplicada a `translateUI()` en `changeLanguage()` (la que estaba después de `localStorage.setItem`).

### Build
- **Command:** `npm run build`
- **Result:** Success (638ms)
- **Output:** 1 page built, no errors, no warnings
- **Files changed:** 3 files, +12/-11 lines

## Sesión 45 — CSS cleanup: duplicates, dead code, reorganization

### Prompt
Analyze the full CSS, identify duplicated selectors (`.content` was duplicated), merge rules that don't change functionality, remove dead CSS, and reorganize into clearly commented sections.

### Plan
1. Merge `.content` (2 base rules → 1)
2. Remove dead CSS: Contact section (~60 lines), Tech Skills Grid (~60 lines), unused `@keyframes`, redundant mobile overrides
3. Remove redundant `.page, .page.exiting` width in `@media (max-width: 480px)` (already in 1235px)
4. Reorganize entire file into 19 numbered sections with detailed comments

### Changes
- **src/styles/global.css**: Complete reorganization (348 insertions, 362 deletions, net -14 lines)
  - Merged `.content` opacity/transition (loading) with layout properties into one rule
  - Removed dead Contact section CSS: `.contact-section`, `.contact-subtitle`, `.contact-buttons`, `.contact-btn`, `.contact-btn-primary`, `.contact-btn-secondary` + hover states + mobile override
  - Removed dead Tech Skills Grid CSS: `.skills-section`, `.skills-grid`, `.skill-tech-item` + all children + mobile overrides
  - Removed unused `@keyframes slide-in-right` and `slide-out-left`
  - Removed redundant `.page, .page.exiting` width override in `@media (max-width: 480px)`
  - Added 19 section comments: 0.DesignTokens, 1.Base, 2.Loading, 3.ScrollReveal, 4.Particles, 5.Layout, 6.Sidebar (6a-6f), 7.MobileToggle, 8.PageSystem, 9.SectionTitles, 10.About, 11.Skills, 12.CardSystem, 13.ProjectCard, 14.BackToTop, 15.Footer, 16.StateClasses, 17.LargeScreens, 18.Mobile, 19.Accessibility
  - Every selector now has a comment explaining what it controls

### Build
- **Command:** `npm run build`
- **Result:** Success (647ms)
- **Output:** 1 page built, no errors, no warnings
- **Files changed:** 1 file, +348/-362 lines
